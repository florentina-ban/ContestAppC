using NUnit.Framework;
using ContestAppC.Repository;
using ContestAppC.Domain;

namespace ContestTest
{
    public class RepoAgeCatTest
    {
        [SetUp]
        public void Setup()
        {
        }

        [Test]
        public void TestFindOne()
        {
            /*
            RepoAgeCategory repoAgeCategory = new RepoAgeCategory();
            AgeCategory cat=repoAgeCategory.FindOne(1);
            System.Console.WriteLine(cat);
            Assert.AreEqual(cat.Name,"6-8");
            */
            Assert.Pass();
        }
    }
}